[build]
  command = "npm run build"
  publish = ".next"

# Modern Next.js is deployed by Netlify through its automatically managed
# OpenNext adapter. Do not pin @netlify/plugin-nextjs unless a rollback to a
# specific adapter version is intentionally required.
